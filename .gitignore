# Rascunhos locais do render_carousel.py (--out tmp/carrossel).
# As artes DE PUBLICAÇÃO vivem em media/carrossel/ e SÃO commitadas de propósito:
# a Graph API do Instagram só aceita `image_url` publicamente acessível, então o
# Pages é o nosso host de mídia. São material de marketing, feitos para ser públicos.
tmp/carrossel/

# Gravações de demo (roteiro em project-money/docs/factories/f-video/scripts/
# demo-capture-founder-proof.md). Mesma razão: peso + exposição pública.
tmp/*.mp4
tmp/*.mov
tmp/*.webm

__pycache__/
*.pyc
